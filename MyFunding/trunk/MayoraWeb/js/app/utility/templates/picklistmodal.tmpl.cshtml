@using Core.Web.Helpers

<div class="modal-header">
    <h3><i class="fa fa-search"></i>Search {{title}}</h3>    
</div>

<div class="modal-body">
    <div style="margin-bottom: 5px;">
        <a class="btn btn-sm btn-danger" ng-click="close()"> <span class="glyphicon glyphicon-chevron-left"></span>  Close</a>
    </div>

    <div class="row">
        <div class="col-lg-12">
            @Html.Angular().AlertGlobal("vm.message")
        </div>
    </div>

    <mvc-grid grid-data="listService"
              title="{{title}}"
              message="vm.message"
              select-column="true"
              table-actions="tableActions"
              sort-by="sortBy"
              search-params="searchParams"
              refresh-data="refreshData"
              show-columns="showColumns"
              hide-columns="hideColumns" />
    @*<div>       
        <h4><i class="fa fa-pie-chart fa-fw"></i> {{vm.title}}</h4>
        '<div>           
           <p ng-if="vm.loading">Loading...</p>
            <div external-scopes="vm.externalScopes" ui-grid="vm.gridOptions" ui-grid-pagination loading="vm.loading">               
                <div class="grid-msg-overlay" ng-hide="!vm.loading">                    
                    <div class="msg">                     
                        <span>Loading Data...<i class="fa fa-spinner fa-spin"></i></span>                        
                    </div>                  
                </div>
                <div class="grid-msg-overlay" ng-hide="vm.loading || vm.gridOptions.data.length">                  
                    <div class="msg">                       
                        <span>No Data</span>                      
                    </div>
                </div>
            </div>
        </div>
    </div>*@

    @*<div class="row">*@
        @*<div class="col-lg-4">
            <div class="panel panel-primary">
                <div class="panel-heading"> Export to Excel</div>
                <div class="panel-content">
                    <a href="#" class="btn btn-info btn-sm" ng-click="exportToExcel('#table1')">
                        <span class="glyphicon glyphicon-share"></span> Export to Excel
                    </a>

                </div>
            </div>
        </div>*@

        @*<div class="col-lg-6">
            <div class="panel panel-info">
                <div class="panel-heading">PageSize </div>
                <div class="panel-content">
                    <select ng-model="$parent.entryLimit" class="form-control">
                        <option>5</option>
                        <option>10</option>
                        <option>20</option>
                        <option>50</option>
                        <option>100</option>
                    </select>
                </div>
            </div>
        </div>*@

        @*<div class="col-lg-6">
            <div class="panel panel-warning">
                <div class="panel-heading">
                    Filtered {{ filtered.length }} of {{ totalItems}} total data
                </div>
                <div class="panel-content">
                    <input type="text" class="form-control" ng-model="searchValue" ng-change="filter()" placeholder="Filter" />
                </div>
            </div>
        </div>*@
    @*</div>*@

    <!--<div class="table-responsive">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <h5>{{title}} List</h5>
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-md-12" ng-show="filteredItems > 0">
                        <table class="table table-striped table-bordered">
                            <tr ng-show="filteredItems==0"><td style="vertical-align:middle;"><i class="fa fa-ban fa-3x"></i>&nbsp;No data found</td></tr>
                            <tr ng-hide="filteredItems>-1"><td style="vertical-align:middle;"><i class="fa fa-cog fa-3x fa-spin"></i>&nbsp;Loading</td></tr>
                            <thead>
                                <th>Select</th>
                                <th ng-repeat="columnName in list.cols">{{columnName}}</th>
                            </thead>
                            <tbody>                                
                                @*<tr ng-repeat="row in list.rows">*@
                                <tr ng-repeat="row in filtered = (list.rows | filter:searchValue | orderBy : predicate :reverse) | startFrom:(currentPage-1)*entryLimit | limitTo:entryLimit">
                                    <td>
                                        <div class="btn-group">
                                            <button type="button" class="btn btn-default fa fa-edit" ng-click="select(row)"></button>
                                        </div>
                                    </td>
                                    <td ng-repeat="column in list.cols">{{row[column]}}</td>
                                </tr>

                                @*<tr ng-repeat="c in $parent.filtered = (list | filter:searchValue | orderBy : predicate :reverse) | startFrom:(currentPage-1)*entryLimit | limitTo:entryLimit">
                                    <td>
                                        <div class="btn-group">
                                            <button type="button" class="btn btn-default fa fa-edit" ng-click="select(c);"></button>
                                        </div>
                                    </td>
                                    <td>{{c.id}}</td>
                                    <td>{{c.nama}}</td>
                                    <td>{{c.pob}}</td>
                                    <td>{{c.dob}}</td>
                                    <td>{{c.address}}</td>
                                    <td>{{c.handphone}}</td>
                                    <td>{{c.phone}}</td>
                                </tr>*@
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>-->

</div>

<div class="modal-footer">

</div>
